version: "3.7"

services:
  slate-frontend:
    image: slate-frontend
    build:
      context: .
      dockerfile: "./Dockerfile"
    ports:
      - "80:80"

networks:
  default:
    external:
      name: aqua
